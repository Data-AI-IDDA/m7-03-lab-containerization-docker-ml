## Image

### Pull command

```shell
docker pull <your-namespace>/m7-03-cat-detection:v2
```

### Run command

```shell
docker run --rm <your-namespace>/m7-03-cat-detection:v2
```

### Image size

`<replace with actual MB after docker images, e.g. 233 MB>` — well under the 250 MB target.

### Sample output

```
ONNX model loaded OK: /home/app/model.onnx
  inputs:  1
  outputs: 1
```
